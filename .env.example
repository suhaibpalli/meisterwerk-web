# Preview lock — see src/lib/preview.ts
#
#   1  locked everywhere (only the home page is reachable)
#   0  open everywhere
#   unset  locked in production, open in development  <- default
#
# Copy this file to .env.local to override locally. Restart the dev server
# after changing it; Next only reads env files at startup.
PREVIEW_LOCK=0
